---

sudo: false
language: python

python:
  - 3.7-dev

matrix:
    include:
        - os: linux
              dist: bionic
        - os: windows

install:
  - pip install -U pandas
  - pip install -U pylint

git:
  submodules: false

script:
  - unittest
